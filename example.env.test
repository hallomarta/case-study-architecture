# Test Environment Configuration
# Copy to .env.test and adjust values as needed

# Server Configuration
PORT=9000

# Database Configuration
DATABASE_URL="postgresql://postgres:development@localhost:5432/postgres?schema=public"

# JWT Configuration - Short expiration times for testing token expiry flows
JWT_SECRET=test-jwt-secret-do-not-use-in-production
JWT_REFRESH_SECRET=test-refresh-secret-do-not-use-in-production
JWT_EXPIRES_IN=5s
JWT_REFRESH_EXPIRES_IN=10s

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000
